Nest 规则小结：在哪个模块里用 constructor 注入某个 provider，那个 provider 必须：在本模块 providers 里，或来自本模块 imports 的、且在其定义模块里被 export 的模块。仅靠「别的模块也 import 了 Memory」不能让你在 Orchestration 里自动注入 Memory 的服务，除非对方再 export 出来或你也 import MemoryModule。
